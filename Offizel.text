<!DOCTYPE html>
<html lang="de">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Kino Frage für Alea 💖</title>
<link href="https://fonts.googleapis.com/css2?family=Pacifico&display=swap" rel="stylesheet">

<style>
    body {
        margin: 0;
        height: 100vh;
        display: flex;
        justify-content: center;
        align-items: center;
        background: linear-gradient(135deg, #ff6fae, #ff9fcf); /* Design aus Datei 1 & 2 kombiniert */
        font-family: Arial, sans-serif;
        overflow: hidden;
    }

    /* iPhone-Stil Container */
    .phone {
        width: 390px;
        height: 844px;
        background: rgba(255, 255, 255, 0.2);
        border-radius: 30px;
        overflow: hidden;
        position: relative;
        box-shadow: 0 20px 40px rgba(0,0,0,0.2);
        display: flex;
        flex-direction: column;
        align-items: center;
        padding: 20px;
        box-sizing: border-box;
    }

    /* Video immer sichtbar */
    .video-container {
        margin-top: 40px;
        width: 100%;
        border-radius: 15px;
        overflow: hidden;
        border: 3px solid white;
    }

    video {
        width: 100%;
        display: block;
    }

    .question {
        text-align: center;
        color: white;
        font-size: 24px;
        font-weight: bold;
        margin: 30px 10px;
        font-family: 'Pacifico', cursive; /* Romantische Schrift */
    }

    /* Button-Bereich */
    .buttons {
        flex-grow: 1;
        width: 100%;
        display: flex;
        justify-content: center;
        gap: 20px;
        align-items: center;
        position: relative;
    }

    button {
        border: 2px solid white;
        color: white;
        font-size: 20px;
        padding: 15px 30px;
        border-radius: 12px;
        cursor: pointer;
        transition: transform 0.3s ease, background-color 0.3s ease;
        z-index: 10;
    }

    #ja {
        background: #2ecc71;
    }

    #nein {
        background: #e74c3c;
    }

    /* End-Screen Overlay */
    #ende {
        display: none;
        position: absolute;
        inset: 0;
        background: linear-gradient(135deg, #ff4d6d, #ff9a9e);
        z-index: 100;
        flex-direction: column;
        justify-content: center;
        align-items: center;
        text-align: center;
        font-family: 'Pacifico', cursive;
        color: white;
    }

    #ende h1 { font-size: 40px; margin: 0; }
    #ende p { font-size: 20px; }

    /* Herz-Animation aus Datei 2 */
    .float {
        position: fixed;
        font-size: 26px;
        z-index: 1000;
        animation: float 2.5s linear forwards;
    }

    @keyframes float {
        from { transform: translateY(0); opacity: 1; }
        to { transform: translateY(-250px); opacity: 0; }
    }
</style>
</head>
<body>

<div class="phone">
    <div class="video-container">
        <video autoplay muted loop playsinline>
           <source src="video.mp4" type="video/mp4">
        </video>
    </div>

    <div id="main-content">
        <div class="question">
            Willst du mit mir ins Kino Budalliza jem? 💖
        </div>

        <div class="buttons">
            <button id="ja">JA</button>
            <button id="nein">NEIN</button>
        </div>
    </div>

    <div id="ende">
        <h1>Bis gleich Alea ❤️</h1>
        <p>von deinem Schatz Enis 💕</p>
    </div>
</div>

<script>
    let jaScale = 1;
    let neinScale = 1;
    let neinClicks = 0;

    const jaBtn = document.getElementById("ja");
    const neinBtn = document.getElementById("nein");
    const endeScreen = document.getElementById("ende");

    // "Nein" Logik: JA wird größer, NEIN wird kleiner
    neinBtn.addEventListener("click", () => {
        neinClicks++;
        jaScale += 0.7; // JA wächst stark
        neinScale -= 0.15; // NEIN schrumpft

        if (neinScale < 0.3) neinScale = 0.3;

        jaBtn.style.transform = `scale(${jaScale})`;
        neinBtn.style.transform = `scale(${neinScale})`;

        // Nach 5 Klicks wird JA zum Vollbild-Button
        if (neinClicks >= 5) {
            neinBtn.style.display = "none";
            jaBtn.style.position = "absolute";
            jaBtn.style.width = "100%";
            jaBtn.style.height = "100%";
            jaBtn.style.top = "0";
            jaBtn.style.left = "0";
            jaBtn.style.transform = "scale(1.5)";
            jaBtn.style.opacity = "0.8";
        }
    });

    // "Ja" Logik: Finale einleiten
    jaBtn.addEventListener("click", () => {
        endeScreen.style.display = "flex";
        
        // Herz & Rosen Regen starten
        setInterval(() => spawn("❤️"), 200);
        setInterval(() => spawn("🌹"), 400);
        setInterval(() => spawn("🍿"), 600);
    });

    // Partikel-Funktion (Herzen/Rosen)
    function spawn(symbol) {
        const el = document.createElement("div");
        el.className = "float";
        el.innerHTML = symbol;
        el.style.left = Math.random() * window.innerWidth + "px";
        el.style.top = (window.innerHeight - 50) + "px";
        document.body.appendChild(el);
        setTimeout(() => el.remove(), 2500);
    }
</script>

</body>
</html>
